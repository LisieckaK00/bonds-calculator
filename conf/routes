# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET /api/data           controllers.DataController.getData
GET /api/bonds/:bondName/:period           controllers.DataController.getByName(bondName: String, period: Int)